// Verificar se o índice de volume pode ser decrementado
if (indexVolume > 0) {
    indexVolume--;
}

// Atualizar o volume com base no índice
volume = volumeVisual[indexVolume];
volumeAtual = volume;